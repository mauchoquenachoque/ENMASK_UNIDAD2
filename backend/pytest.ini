[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --tb=short --strict-markers -x
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow tests
filterwarnings =
    ignore::DeprecationWarning
